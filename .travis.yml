language: android
dist: focal
group: previous
android:
  components:
    - build-tools-30.0.0
    - android-30
    - extras;google;google_play_services
    - extras;google;m2repository
    - extras;android;m2repository

before_script:
  - echo "Przygotowywanie środowiska przed kompilacją..."
  - ant -version || { echo "Ant nie jest zainstalowany lub nie jest w PATH"; exit 1; }

script:
  - echo "Zakończenie kompilacji..."
