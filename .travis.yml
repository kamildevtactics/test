os: linux
dist: focal
group: dev

services:
  - podman

before_install:
  - podman pull docker.io/nginx:latest
  - podman run --privileged -d -p 127.0.0.1:80:80 docker.io/nginx:latest
  - podman ps -a
  - curl http://127.0.0.1:80 -I
script:
  - echo "test"
