os: freebsd

script:
  # Python support
  - python3.9 --version
  - python3.10 --version
  - python3.11 --version
  - python3.12 --version
  - python3.13 --version
  - pip3 --version
  - pypy --version

