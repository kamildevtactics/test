jobs:
  include:
  - dist: focal
  - dist: focal
    group: previous
    
services:
- postgresql
addons:
  postgresql: '13'
  apt:
    packages:
    - postgresql-13
    - postgresql-client-13
    - postgresql-server-dev-13
    - postgresql-13-postgis-3
    - postgresql-13-postgis-3-scripts
script:
 - psql --version
