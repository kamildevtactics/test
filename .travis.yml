jobs:
  include:
  - dist: bionic
  - dist: bionic
    group: dev
  - dist: bionic
    group: previous
    
services:
- postgresql
addons:
  postgresql: '12'
  apt:
    packages:
    - postgresql-12
    - postgresql-client-12
    - postgresql-server-dev-12
    - postgresql-12-postgis-3
    - postgresql-12-postgis-3-scripts
script:
 - psql --version
