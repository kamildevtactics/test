os: linux
dist: bionic
group: dev

service:
 -podman

jobs:
  include:
    - name: "Check Podman Version on Ubuntu Focal"
      
before_install:
  - sudo apt-get update

script:
  - podman --version
  - echo "Checking Podman info:"
  - podman info
  - podman run --rm hello-world
