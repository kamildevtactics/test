os: linux
arch: amd64
dist: jammy        # Ubuntu 22.04

cache:
  directories:
    - ~/.npm

git:
  depth: 1
  quiet: true
  submodules: false

before_install:
  - node -v
  - npm config set loglevel verbose
  - npm install --verbose

script:
  - echo "npm install completed successfully"
