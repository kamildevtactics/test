os: linux
dist: noble
group: dev

service:
  - docker

jobs:
  include:
    - name: "Check Podman Version on Ubuntu Bionic"

before_install:
  - sudo apt-get update

script:
  - docker --version
  - docker info
  - docker run --rm hello-world
