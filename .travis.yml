language: shell
dist: bionic
group: dev

services:
  - elasticserach
before_install:
  - until curl --silent -XGET --fail http://localhost:9200/_cluster/health?pretty; do printf '...\n'; sleep 2; done
install: skip
script: true
