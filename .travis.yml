os: linux
dist: focal
group: dev

services:
  - podman

before_install:
  - sudo podman pull docker.io/nginx:latest
  - sudo podman run --privileged -d nginx
  - podman ps -a

script:
  - echo "test"
