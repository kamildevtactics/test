language: android
dist: jammy
sudo: required
jdk: openjdk11

android:
  components:
    - platform-tools
    - build-tools;30.0.0

script:
  - export ANDROID_HOME=/usr/local/android-sdk
  - env
  - sdkmanager --version
