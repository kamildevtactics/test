language: android
dist: bionic

android:
  components:
    - platform-tools
    - build-tools;30.0.0
    - platforms;android-30

script:
  - ls -ld /usr/local/android-sdk/cmdline-tools/latest/bin
  - which sdkmanager
