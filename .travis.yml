dist: bionic
group: dev

before_script:
  - echo "Przygotowywanie środowiska przed kompilacją..."
  - ls -la /usr/local/android-sdk/android-sdk
  - ls -la /usr/local/android-sdk
  - sudo chmod -R 777 /usr/local/android-sdk/android-sdk
  - sudo chmod -R 777 /usr/local/android-sdk/android-sdk
  - sudo chmod -R 777 /usr/local/android-sdk
  - sdkmanager --list


