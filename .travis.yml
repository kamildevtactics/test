dist: bionic
group: dev

before_script:
  - echo "Przygotowywanie środowiska przed kompilacją..."
  - if [ -d "/usr/local/android-sdk/cmdline-tools/" ]; then echo "Android cmdline-tools directory exists."; else echo "WARNING: /usr/local/android-sdk/cmdline-tools/ not found!"; fi

script:
  - echo "Zakończenie kompilacji..."
