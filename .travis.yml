language: android
dist: focal
android:
  components:
    # (Optional) Uncomment these if you want to use the latest Android SDK Tools
    # - tools
    # - platform-tools

    # BuildTools version recommended for API 30
    - build-tools;30.0.3

    # Android SDK Platform for API 30
    - platforms;android-30

    # Additional components
    - extras;google;google_play_services
    - extras;google;m2repository
    - extras;android;m2repository

    # System images for running emulators during tests
    - system-images;android-30;google_apis;x86_64
