jobs:
  include:
  - dist: focal
  - dist: focal
    group: dev
  - dist: focal
    group: previous
    
services:
- postgresql
addons:
  postgresql: '12'
  apt:
    packages:
    - postgresql-12
    - postgresql-client-12
    - postgresql-server-dev-12
    - postgresql-12-postgis-3
    - postgresql-12-postgis-3-scripts
script:
 - psql --version
