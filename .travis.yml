os: linux
dist: focal
group: dev

services:
  - podman

before_install:
- sudo podman pull docker.io/nginx:latest
- sudo podman run --privileged -d -p 127.0.0.1:80:8080 docker.io/nginx:latest
- podman ps -a
- podman run --rm docker.io/nginx:latest /bin/sh -c "nginx -v"

script:
  - echo "test"
