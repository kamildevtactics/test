dist: focal
language: php
php: 8.2

install: 
- rvm use 3 --fuzzy do ruby -S gem install dpl -v
script:
  - ruby -v

deploy:
  provider: script
  script: sleep 10
  skip_cleanup: true
  on:
    all_branches: true
