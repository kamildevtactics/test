os: windows
language: shell
before_install:
  - choco install golang --version=1.20.0
  - export GOROOT="/c/Go"
  - export PATH="$GOROOT/bin:$PATH"
script:
  - go version
  - go build
  - go test ./...
