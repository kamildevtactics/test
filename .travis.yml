node_js:
  - 20.9
os: linux
arch: amd64
dist: jammy
group: dev

cache:
  directories:
    - ~/.npm
git:
  depth: 1
  quiet: true
  submodules: false

before_install:

install:
  - npm install
script:
  - echo "npm install completed successfully"
