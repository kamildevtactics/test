language: android
dist: bionic

android:
  components:
    - build-tools-26.0.2
    - android-26
    - extra

before_install:
  - sudo apt update
  - sudo apt install -y ant

script:
  - ant debug install test
